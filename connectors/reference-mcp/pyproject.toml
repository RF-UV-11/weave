[project]
name = "weave-reference-mcp"
version = "0.1.0"
description = "Reference MCP server (fake booking system) to develop the orchestrator's MCP client against"
requires-python = ">=3.12"
dependencies = [
  "mcp>=2.0.0",
  "uvicorn>=0.27.0",
]
