# Dev-only values — the same ones committed in infra/podman-compose.yml.
# Generate real secrets for anything beyond a local minikube prototype
# (`openssl rand -base64 32` for VAULT_ROOT_KEY, any strong random string
# for JWT_SECRET) and manage them via `kubectl create secret` or a real
# secrets manager instead of committing them, once this stops being a
# throwaway prototype. See docs/architecture/SECURITY.md §3.
apiVersion: v1
kind: Secret
metadata:
  name: core-secrets
  namespace: weave
type: Opaque
stringData:
  VAULT_ROOT_KEY: "5eJ9k0h2xW1qzT7yD3mN8pR4sL6vC9bA0fG2hJ5kM8o="
  JWT_SECRET: "dev-only-jwt-secret-do-not-use-in-prod"
