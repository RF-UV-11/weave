# Copy to .env.local (gitignored) and adjust if your Envoy proxy isn't on
# the default port. See infra/envoy/envoy.yaml (container, matches
# podman-compose.yml) or infra/envoy/envoy.local.yaml (native binary —
# Windows/WSL2 dev workaround, see that file's comment) for how to run it.
NEXT_PUBLIC_ENVOY_URL=http://localhost:8090
